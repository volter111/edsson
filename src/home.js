import React from "react";
import LayoutPage from "./layoutPage";
import { Center } from "@mantine/core";

const Home = () => {
  return (
    <Center>
        <LayoutPage />
    </Center>
  );
};

export default Home;
